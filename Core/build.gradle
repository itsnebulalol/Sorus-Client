plugins {
    id 'java'
    id "com.github.sherter.google-java-format" version "0.8"
}

repositories {
    mavenCentral()
    jcenter()
    maven { url 'https://jitpack.io' }
}

configurations {
    jarExport
    compile.extendsFrom(jarExport)
}

dependencies {
    jarExport group: 'org.ow2.asm', name: 'asm-all', version: '5.2'
    jarExport group: 'com.google.code.gson', name: 'gson', version: '2.8.0'
    jarExport group: 'commons-io', name: 'commons-io', version: '2.5'
    jarExport group: 'com.cedarsoftware', name: 'json-io', version: '4.12.0'
    jarExport group: 'com.jagrosh', name: 'DiscordIPC', version: '0.4'
    jarExport group: 'javazoom', name: 'jlayer', version: '1.0.1'
    jarExport 'com.github.sealedtx:java-youtube-downloader:2.4.1'
    jarExport group: 'net.sourceforge.jaadec', name: 'jaad', version: '0.8.5'
    compile group: 'com.google.guava', name: 'guava', version: '30.1-jre'
}

jar {
    from {
        configurations.jarExport.collect { it.isDirectory() ? it : zipTree(it) }
    }
}